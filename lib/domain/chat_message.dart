class ChatMessage {
  ChatMessage(this.id, this.message, this.senderId, this.time, this.sendImg, this.height, this.width, this.name, this.size, this.sendFile, this.mimeType);
  String id;
  String message;
  String senderId;
  int time;
  String sendImg;
  int height;
  int width;
  String name;
  int size;
  String sendFile;
  String mimeType;
}