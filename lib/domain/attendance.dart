class Attendance {
  Attendance(this.id, this.title, this.start, this.end, this.description, this.mailSend, this.userId, this.color);
  String id;
  String title;
  DateTime start;
  DateTime end;
  String description;
  bool mailSend;
  String userId;
  int color;
}

