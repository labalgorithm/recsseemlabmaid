class Member {
  Member(this.id, this.email, this.grade, this.group, this.name, this.uid, this.status);
  String id;
  String email;
  String grade;
  String group;
  String name;
  String uid;
  String status;
}